# GitHub Repository Setup Commands

# After creating your GitHub repository, run these commands:

# 1. Add the remote repository (replace YOUR_USERNAME with shubhamp2504)
git remote add origin https://github.com/shubhamp2504/fyers-automated-trading.git

# 2. Push all code to GitHub
git branch -M main
git push -u origin main

# Alternative SSH method (if you have SSH keys set up):
# git remote add origin git@github.com:shubhamp2504/fyers-automated-trading.git
# git push -u origin main